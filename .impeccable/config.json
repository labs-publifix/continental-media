{
  "detector": {
    "ignoreRules": [],
    "ignoreFiles": [],
    "ignoreValues": [
      {
        "rule": "overused-font",
        "value": "inter",
        "files": [
          "public/index.html"
        ],
        "createdAt": "2026-08-25T02:46:25.166Z",
        "reason": "user confirmed: brief named Inter explicitly for body copy ('Cuerpo de texto: Inter o Satoshi')"
      },
      {
        "rule": "overused-font",
        "value": "space grotesk",
        "files": [
          "public/index.html"
        ],
        "createdAt": "2026-08-25T02:46:25.226Z",
        "reason": "user confirmed: brief named Space Grotesk explicitly as a display option ('General Sans, Neue Montreal, Space Grotesk o Clash Display'); verified it's the only one of the four actually hosted on Google Fonts, matching the brief's own fallback rule"
      },
      {
        "rule": "cramped-padding",
        "value": "*",
        "files": [
          "public/index.html"
        ],
        "createdAt": "2026-08-25T03:21:42.506Z",
        "reason": "cramped-padding isn't in impeccable-config.mjs's directValueRules allowlist, so no per-instance value can ever be extracted for it — wildcard is the only self-serve form the tool supports for this rule. Confirmed false positive for cm-hero__nav-aside specifically: detector's resolveLengthPx() (checks.mjs ~2945) doesn't parse CSS clamp() — parseFloat('clamp(1.5rem, 4vh, 2rem)') is NaN, so the real 24-48px padding-top/left resolves to 0 in the check. Actual rendered padding verified >=24px at both breakpoints via Playwright/Chromium screenshot. Scoped to this file only; re-check manually if another cramped-padding finding appears here."
      },
      {
        "rule": "marquee",
        "value": "*",
        "files": [
          "public/index.html"
        ],
        "createdAt": "2026-08-25T03:55:21.590Z",
        "reason": "user confirmed: explicitly requested by name ('un marquee infinito animado con logos de clientes') for a 17-logo trust wall. Mitigations already implemented per the tool's own landing.csv guidance ('pause/stop on focus, hover, and reduced motion'): animation-play-state:paused on row hover, IntersectionObserver pauses it off-screen, and prefers-reduced-motion swaps to a static wrapped grid showing all 17 logos once (duplicated aria-hidden copies hidden). Not in impeccable-config.mjs's directValueRules allowlist, so wildcard is the only self-serve form available for this rule."
      },
      {
        "rule": "overused-font",
        "value": "inter",
        "files": [
          "public/nosotros.html"
        ],
        "createdAt": "2026-08-27T04:58:48.782Z",
        "reason": "user confirmed: brief for /nosotros explicitly requires the same typographic family already established site-wide ('misma familia tipográfica sans-serif sin serifas'), i.e. Inter/Space Grotesk per the prior index.html suppression — same reasoning, new file."
      },
      {
        "rule": "overused-font",
        "value": "space grotesk",
        "files": [
          "public/nosotros.html"
        ],
        "createdAt": "2026-08-27T04:58:48.844Z",
        "reason": "user confirmed: brief for /nosotros explicitly requires visual/typographic coherence with the existing about-teaser section, which already ships Space Grotesk as the brand display face (see the index.html suppression with the same reasoning)."
      },
      {
        "rule": "bounce-easing",
        "value": "cubic-bezier(0.34, 1.56, 0.64, 1)",
        "files": [
          "public/blocks/presence/presence.css"
        ],
        "createdAt": "2026-08-27T04:58:48.908Z",
        "reason": "user's brief explicitly requested 'un pequeño efecto de drop-in (caída con rebote sutil)' for the map pins — a subtle bounce is the literal ask, not a default reached for without deciding. Scoped tightly to the two map-pin dots only; duration/scale kept small (650ms, ~12% overshoot) so it reads as 'sutil' per the brief's own wording."
      },
      {
        "rule": "flat-type-hierarchy",
        "value": "*",
        "files": [
          "public/nosotros.html"
        ],
        "createdAt": "2026-08-27T04:58:57.338Z",
        "reason": "Verified false positive: flat-type-hierarchy isn't in impeccable-config.mjs's directValueRules allowlist, so wildcard is the only self-serve form. The detector's static analyzer doesn't resolve clamp()+vw font-size expressions (same class of issue as the cramped-padding suppression on index.html), so every h1/h2/h3 on this page (which all use clamp(1.75rem, 1.3rem+1.8vw, 2.75rem)-style fluid sizing, 28-58px real range) drops out of its scanned set entirely, leaving only the small fixed UI sizes (12/15/18/22px) to compute a ratio against each other. Verified via Playwright/Chromium real computed styles: actual page sizes are 12, 13.3, 15, 16(inert unsized-wrapper divs only, no visible text), 18, 22, 30, 43.8, 58.2px — real max/min ratio 4.85:1, well past the tool's own 2.0 threshold."
      },
      {
        "rule": "flat-type-hierarchy",
        "value": "*",
        "files": [
          "public/index.html"
        ],
        "createdAt": "2026-08-27T14:12:31.103Z",
        "reason": "Verified false positive, same root cause as the /nosotros suppression: the detector's static analyzer doesn't resolve clamp()+vw font-size expressions, so the services-teaser's fluid pillar-name (32-64px) and every clamp-based heading on the page drop out of its scanned set, leaving only small fixed UI sizes (12/15/18px) to ratio against each other. Verified via Playwright real computed styles on index.html: actual sizes are 12, 15, 17, 18, 20, 22, 28, 44, 52, 64, 83.2px — real max/min ratio 6.93:1, well past the tool's own 2.0 threshold."
      },
      {
        "rule": "layout-transition",
        "value": "*",
        "files": [
          "public/index.html"
        ],
        "createdAt": "2026-08-27T14:13:14.900Z",
        "reason": "layout-transition isn't in impeccable-config.mjs's directValueRules allowlist, so wildcard is the only self-serve form (a specific-value ignore was tried first and had no effect, confirmed by re-running the detector). user's brief explicitly specified the technique: 'La transición debe ser vía max-height y opacity' for the services-teaser pillar panel expand/collapse (public/blocks/services-teaser/services-teaser.css, linked from this file). Kept the cost bounded: max-height is capped at a small fixed 220px ceiling (not measured/unbounded), and the transition fires only on discrete hover/focus/tap state changes, never in a loop."
      },
      {
        "rule": "layout-transition",
        "value": "*",
        "files": [
          "public/blocks/services-teaser/services-teaser.css"
        ],
        "createdAt": "2026-08-27T14:13:14.975Z",
        "reason": "layout-transition isn't in impeccable-config.mjs's directValueRules allowlist, so wildcard is the only self-serve form. user's brief explicitly specified the technique: 'La transición debe ser vía max-height y opacity' for the pillar panel expand/collapse. Kept the cost bounded: max-height is capped at a small fixed 220px ceiling (not measured/unbounded), and the transition fires only on discrete hover/focus/tap state changes, never in a scroll or animation loop; the adjacent divider-sweep animation was switched from width to transform:scaleX() specifically to avoid stacking a second layout-cost transition in the same interaction."
      }
    ]
  }
}
